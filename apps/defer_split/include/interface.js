var defer_split = {
	spot : {
		dialog_split : fn.noaccess,
		append_split : fn.noaccess,
		dialog_remove : fn.noaccess,
		remove : fn.noaccess,
		split : fn.noaccess
	}
};
$.extend(fn.app,{defer_split:defer_split});
