var repack = {
	repack : {
		dialog_lookup : fn.noaccess,
		dialog_combine : fn.noaccess,
		dialog_split : fn.noaccess,
		combine : fn.noaccess,
		split : fn.noaccess
	},
};
$.extend(fn.app,{repack:repack});
