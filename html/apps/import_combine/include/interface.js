var import_combine = {
	combine : {
		dialog_lookup : fn.noaccess,
		dialog_add : fn.noaccess,
		dialog_edit : fn.noaccess,
		dialog_split : fn.noaccess,
		dialog_remove : fn.noaccess,
		dialog_lookup : fn.noaccess,
		add : fn.noaccess,
		edit : fn.noaccess,
		remove : fn.noaccess,
		split : fn.noaccess,
		calcuate : fn.noaccess
	}
};
$.extend(fn.app,{import_combine:import_combine});
