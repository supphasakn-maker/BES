var delivery = {
	delivery : {
		dialog_lookup : fn.noaccess,
		dialog_prepare : fn.noaccess,
		dialog_delivery : fn.noaccess,
		dialog_lookup : fn.noaccess,
		prepare : fn.noaccess,
		delivery : fn.noaccess,
		lookup : fn.noaccess,
		prepare_append_driver : fn.noaccess,
	},
};
$.extend(fn.app,{delivery:delivery});
